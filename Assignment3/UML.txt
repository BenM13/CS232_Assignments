CLASS: Item
    - Attributes:
        -- int priority
        -- double price
        -- String name
        -- boolean purchased
    Methods:
        -- Contructor
        -- setPriority
        -- setPrice
        -- setPurchased // change the status of purchased
        -- equals, //check for duplicates for both name and priority
        -- get methods for each attribute 

CLASS: Utilities
    - Attributes:
        -- int newInt
        -- char newChar
        -- final String PROMPT // for easy to read console
        -- final String REGEX // pattern for regular expression
    - Methods:
        -- static parse character
        -- static parse integer
        -- static print money format (from textbook)
        -- static quitProgram
        -- static sort items

